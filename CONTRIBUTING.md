# 论文仓库协作规范

## 开始任务

1. 在任务单写明负责人、允许修改的文件和验收条件。
2. 从最新 `main` 创建分支：`paper/<任务号>-<简短描述>`。
3. 开工前确认没有其他人同时编辑相同章节或主文档。

```bash
git switch main
git pull --ff-only origin main
git switch -c paper/12-q2-method
```

## 提交与审核

- 只暂存本任务文件，不使用无检查的 `git add .`。
- 推荐提交格式：`paper(q2): describe optimization model`。
- 合并前同步最新 `main`，运行 `scripts/check.ps1` 并完成论文编译。
- PR 必须由另一位成员审核；每次只合并一个 PR。
- 使用 Squash and merge，合并后删除任务分支。

## 文件边界

- `main.tex`、全局格式和参考文献库由论文总负责人维护。
- 每道题的章节和图片目录指定唯一负责人。
- `.doc`、`.docx`、`.pdf`、图片等二进制文件无法可靠自动合并；禁止多人覆盖同一路径。
- 临时编译文件、Office 锁文件和自动生成目录不得提交。
- 代码结果先合入 `Main`，论文 PR 再记录来源提交哈希和复现命令。

## 冲突处理

冲突只能在任务分支解决，禁止在 `main` 上临时修改。文本冲突由 PR 作者与文件负责人共同确认；二进制冲突由文件负责人选择基准版本，再人工迁移另一版本的内容。
